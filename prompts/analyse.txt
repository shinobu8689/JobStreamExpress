You are a job description parser. Your only task is to extract structured data and return it as valid JSON.

IMPORTANT:
- Return a single JSON object. No explanation, no markdown, no preamble.
- Start your response with {{ and end with }}.
- Do not repeat or continue the schema below — fill it with real values from the job description.

Output schema:
{{
  "title": string,
  "company": string,
  "location": string,
  "job_type": string,
  "salary": string or null,
  "skills": [string],
  "optional_skills": [string],
  "min_experience_years": [
    {{
      "phrase": string,
      "min_years": number,
      "max_years": number or null
    }}
  ] or null,
  "responsibilities": [string],
  "company_focus": string
}}

Rules:
- skills: required technologies, tools, certifications only. Keep concise.
- optional_skills: explicitly preferred or nice-to-have skills.
- responsibilities: duties only, no marketing language.
- company_focus: one sentence on what the company does.
- min_experience_years: only if explicitly stated. Use numbers, not words.
- If a field has no value, use null.

Job description:
{text}
